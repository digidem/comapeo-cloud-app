---
type: sweep
title: Doc Drift Sweep 2026-07-20
date: 2026-07-20
---

# Doc Drift Sweep 2026-07-20

## What was checked

`origin/main` is unchanged at `35c89ab` — byte-identical to the commit audited in
the 2026-07-19 baseline sweep. No code moved, so this sweep spent its effort
adjudicating the pre-scan's broken-path candidates rather than re-auditing a
tree that could not have drifted.

- All pre-scan `brokenPaths` candidates for `AGENTS.md` and `DESIGN_OVERVIEW.md`
  verified by hand against the working tree, `git ls-files`, and `.gitignore`.
- PR state for the previous sweep (#133) checked.
- `npm test` / `npm run build` **not run** — both were verified green on this
  exact commit on 2026-07-19 and the tree is unchanged, so a rerun carries no
  information. Next sweep on a moved `main` must run them.

## Drift found

1 genuine item (new), 3 candidates dismissed, 1 already covered by open PR #133.

## Fixes applied

N/A — **no PR opened**. Spec rule: previous sweep PR #133
(`docs/drift-sweep-2026-07-19`) is still OPEN and unmerged, so PR creation is
skipped and this sweep only reports. The finding below is queued for the sweep
that follows #133's merge.

## Notes

**New genuine drift — `DESIGN_OVERVIEW.md` "Source Files" section (lines 118-126).**
Every one of its seven bullets points at a path that exists in no clone of this
repo:

| Reference | Reality |
|---|---|
| `DESIGN.md` (root) | does not exist |
| `.stitch/DESIGN.md`, `.stitch/metadata.json`, `.stitch/designs/*.png`, `.stitch/screens/*.md` | no `.stitch/` directory anywhere in the repo |
| `NOTES.md`, `NOTES_1.md` | do not exist |
| `plans/2026-04-18-stitch-execution-plan-v3.md` | `plans/` is gitignored and empty |

This is not a gitignore artifact like `design/` or `.claude/` — `.stitch/` is
absent even locally. The tell is line 125: `` `.stitch/screens/*.md` (this
folder) ``. `DESIGN_OVERVIEW.md` was authored *inside* an external Stitch
workspace's `screens/` directory and copied to the repo root in a single commit
(`ff2d626 chore: add project docs and banner asset`) without rewriting its
relative references. The section is dead weight for any reader.

This is in scope — `DESIGN_OVERVIEW.md` is an undated reference doc on the audit
list, not a dated `docs/superpowers/specs/` or `plans/` snapshot. Suggested fix
is to drop the section (the Stitch workspace is not part of this repo) rather
than repoint it; that needs no maintainer decision, just a PR slot.

Note this section was reported clean in the 2026-07-19 baseline — a miss. The
pre-scan did not exist for that sweep, which is exactly the gap it was added to
close; first sweep with the pre-scan running caught it.

**Candidates dismissed (do not re-flag):**

- `AGENTS.md` → `tests/e2e/screenshots/desktop/`, `tests/e2e/screenshots/mobile/`
  — generated, gitignored artifacts. The same document states this two lines
  later ("Screenshots are gitignored (generated artifacts)"). Intentional prose.
- `AGENTS.md` → `src/screens/ScreenName.stories.tsx` — a placeholder filename in
  a numbered how-to step ("Create `src/screens/ScreenName.stories.tsx`"), not a
  link. Intentional prose.
- `AGENTS.md` → `design/prototype/DESIGN.md` — genuine, but already fixed in the
  pending PR #133. Not re-counted.

**Open maintainer items still unaddressed** (raised 2026-07-19, not re-fixed):
`package-lock.json` out of sync with `package.json` (CI uses Bun, so npm's
lockfile rotted unnoticed); AGENTS.md's Zenith section pointing at untracked
`.claude/` / `.codex/` orchestrator prompts.
