---
type: sweep
title: Doc Drift Sweep 2026-07-19
date: 2026-07-19
---

# Doc Drift Sweep 2026-07-19

Baseline sweep — first run of this loop.

## What was checked

- `README.md`, `AGENTS.md`, `CLAUDE.md`, `DESIGN_OVERVIEW.md`, `SECURITY.md`, `WORKFLOW.md`
- `docs/deployment-runbook.md`, `docs/remote-archive-api-spec.md`, `docs/superpowers/specs/`
- Every path referenced by the docs, checked for existence against `origin/main`
- Every CLI command in the docs, checked against `package.json` scripts
- Documented API endpoints, cross-checked against `src/lib/api-client.ts` and `functions/api/`
- Dependency and tooling versions in README badges vs `package.json`
- `npm test` → **pass** (157 files, 1931 passed, 1 skipped)
- `npm run build` → **pass**

## Drift found

5 fixes across 2 files.

## Fixes applied

- **`src/types/` does not exist** — documented in the structure blocks of both
  `README.md` and `AGENTS.md`; the only `.d.ts` in the tree is `src/vite-env.d.ts`. Removed.
- **"one folder per screen" is wrong** — `src/screens/` holds 9 flat component
  files and only 2 screen folders (`Home/`, `MapScreen/`). Reworded in both files.
- **Store list was stale** — README said `(auth, theme, locale)`; the tree has
  archive, auth, locale, map, project, theme, view-mode.
- **`design/prototype/DESIGN.md` never existed** — AGENTS.md pointed all visual
  decisions at it. `design/` is gitignored and was never tracked in git history;
  the real token reference is `DESIGN_OVERVIEW.md`. Repointed.
- **Hardcoded personal path in the tunnel section** — AGENTS.md told agents to
  `cd /home/coder/comapeo-cloud-app`, a path that does not exist in any worktree.
  A `dev:tunnel` script now automates the whole start-Vite-then-tunnel dance;
  documented that instead.

## Notes

Two findings were **left for a maintainer decision** rather than fixed, since
guessing the intent would have been inventing content:

1. **`npm ci` is broken on `main`.** `package-lock.json` is out of sync with
   `package.json` (`lock file's esbuild@0.27.3 does not satisfy esbuild@0.28.1`).
   It went unnoticed because CI does not use npm — every job runs
   `bun install --frozen-lockfile` against `bun.lock`. So `package-lock.json`
   looks like an unmaintained vestige. Either refresh it or delete it; also worth
   deciding whether README's `npm install` setup step should say Bun, since that
   is what CI actually standardizes on. (`npm install` still works today.)

2. **AGENTS.md's Zenith section points at files that do not exist** —
   `.claude/orchestrator_prompt.md` and `.codex/orchestrator_prompt.md`. Both
   directories are gitignored and neither file was ever tracked, so no clone has
   them. Either the prompts should be committed or the section should be reworded.

Verification note: pushing this repo triggers a pre-push hook that runs the full
`scripts/validate.sh` suite, so pushes take several minutes. That is expected
behavior, not a stall.
