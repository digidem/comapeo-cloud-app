set -eu

BRANCH=$(git rev-parse --abbrev-ref HEAD)

npm run validate

case "$BRANCH" in
  auto/*)
    echo "=== Auto branch: running coverage diff ==="
    npm run test:coverage
    node scripts/coverage-diff.mjs
    ;;
esac
