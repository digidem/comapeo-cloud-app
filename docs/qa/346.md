# PR #346 technical QA — agent knowledge architecture

## Scope

Validate that PR #346 changes only repository agent/process documentation and policy tests, preserves all durable PR #287/#341 lessons in canonical scoped homes, and leaves the existing PR-cycle safety contract operational.

There is no production UI/runtime feature to click through. This handoff is intentionally a technical repository QA procedure.

## Prerequisites

- Check out PR #346 at its exact current head SHA in a clean worktree.
- Python 3 and repository Node dependencies are available.
- Run from repository root.

## 1. Run all PR-cycle helper and policy tests

```bash
python3 .agents/skills/pr-cycle/scripts/test_pr_snapshot.py
python3 .agents/skills/pr-cycle/scripts/test_pr_wait.py
python3 .agents/skills/pr-cycle/scripts/test_claude_review.py
python3 .agents/skills/pr-cycle/scripts/test_reviewer_fallback_policy.py
python3 .agents/skills/pr-cycle/scripts/test_pr_cycle_policy.py
python3 .agents/skills/pr-cycle/scripts/test_agent_knowledge_policy.py
```

Expected result: every suite exits 0. At the time this handoff was authored the aggregate count is 140 tests.

Failure condition: any test failure, especially a missing canonical destination, broken resource link, chronological session-memory file, vendor-policy fork, or regression in PR merge/readiness safeguards.

## 2. Verify formatting and syntax

```bash
npx prettier --check \
  .agents/skills/maintaining-agent-knowledge/SKILL.md \
  .agents/skills/pr-cycle/SKILL.md \
  .agents/skills/pr-cycle/references/qa-evidence.md \
  .agents/skills/pr-cycle/references/review-evidence.md \
  .github/workflows/ci.yml \
  AGENTS.md \
  docs/adr/0001-agent-knowledge-architecture.md \
  docs/adr/README.md \
  docs/qa/346.md \
  docs/superpowers/specs/2026-09-04-agent-knowledge-architecture.md \
  src/lib/map/AGENTS.md \
  tests/e2e/AGENTS.md
python3 -m py_compile \
  .agents/skills/pr-cycle/scripts/test_agent_knowledge_policy.py \
  .agents/skills/pr-cycle/scripts/test_pr_cycle_policy.py
git diff --check origin/main...HEAD
```

Expected result: all commands exit 0.

## 3. Verify chronological session memory was removed

```bash
find .agents -type f -iname '*session*lesson*.md' -print
```

Expected result: no output.

Then inspect `docs/adr/0001-agent-knowledge-architecture.md` and confirm its migration manifest contains every PR #287 and PR #341 source heading with an explicit disposition and canonical destination.

Failure condition: any session-memory file remains, any source heading is absent from the manifest, or a manifest destination does not exist.

## 4. Verify progressive-disclosure routing

Confirm:

- root `AGENTS.md` points session-learning work to `.agents/skills/maintaining-agent-knowledge/SKILL.md`;
- root `AGENTS.md` points E2E/visual work to `tests/e2e/AGENTS.md`;
- `tests/e2e/AGENTS.md` contains only E2E/browser/visual local deltas and links to `qa-evidence.md` for general evidence rules;
- `src/lib/map/AGENTS.md` contains only durable map/SMP local invariants supported by shipped code/tests/QA;
- `.agents/skills/pr-cycle/SKILL.md` delegates durable-knowledge placement to `maintaining-agent-knowledge` instead of carrying a duplicate taxonomy;
- `CLAUDE.md` remains exactly `@AGENTS.md`;
- `.github/copilot-instructions.md` has not been introduced.

Failure condition: contradictory duplicate policy, a missing router link, or vendor-specific repository policy that forks the canonical instructions.

## 5. Verify material lesson preservation

Check the canonical destinations for the high-risk details that were previously easy to lose during consolidation:

- `review-evidence.md`: reviewer findings are hypotheses, confirmed correctness findings should become RED regression tests when practical, and transport/provider failure is not a verdict;
- `qa-evidence.md`: stale E2E selectors/TODOs require current trace/DOM diagnosis, direct deployed-artifact probes supplement rather than replace product-flow QA, exact-SHA browser substitution is constrained, and changed-area validation is rerun after formatting/base synchronization;
- `src/lib/map/AGENTS.md`: resource bounds precede expensive work, untrusted accessors/getters are not executed during validation, archive determinism covers timezone/input order, parser exceptions stay narrow/fail-closed, missing glyph behavior preserves TinySDF fallback, and cancellation covers finalization waits;
- `tests/e2e/AGENTS.md`: responsive tests reacquire current-layout controls and masked browser/CI evidence is inspected rather than inferred from wrapper status.

Failure condition: any listed invariant is absent, weakened, or contradicted by another active instruction.

## 6. Verify CI integration

Inspect `.github/workflows/ci.yml` and confirm the existing `pr-cycle-skill-tests` job name/context is unchanged and now runs:

```bash
python3 .agents/skills/pr-cycle/scripts/test_agent_knowledge_policy.py
```

Expected result: the new policy is enforced under the existing protected context rather than introducing a renamed replacement check.

## Cleanup/reset

No runtime data or external service state is modified by this QA procedure. Remove only incidental local Python cache files or generated artifacts if your tooling creates them; do not clean unrelated worktree state.

## Known limitations

- This PR intentionally does not test production UI behavior because it contains no application runtime changes.
- Git history/PR discussion remains the provenance source for deleted chronological session notes; this QA validates preservation of durable knowledge, not preservation of every historical anecdote.
- A future agent client may justify a vendor-specific compatibility adapter if it demonstrably cannot consume `AGENTS.md`; this PR intentionally does not add one preemptively.
